# Current integration inventory

This inventories the extraction from `rust-v0.154.0`, commit `6b9826e3aa83b1a5947db50f4332cb9c65f1b340`, carried forward from `mcp-console/sandbox-runner/rust-v0.150.1` at `561883728ee639c846b80280fa4ed3ff41d12d69`. Only the pinned release is supported. Use this inventory with the source and [reapplication procedure](REBASE.md) when upgrading.

## What the patch carries

Upstream already supplies filesystem/network policy, the managed proxy, Seatbelt, seccomp, bubblewrap, and namespace-init cleanup. The patch adds a [standalone configuration boundary](PROTOCOL.md), target-environment isolation, optional trusted Seatbelt extensions, and one [supervised lifetime](LIFECYCLE.md) with cancellation, caller-death observation, private storage, and cleanup-error reporting. Native Linux waiting helpers also release target stdin promptly. These additions do not imply that every other upstream execution path lacks process management or cleanup.

The shared policy models, Seatbelt policy files, seccomp rules, proxy implementation, vendored bubblewrap, and helper build recipe remain unchanged. No Console-specific policy defaults, sideband framing, application restart, or watchdog are included. Behavioral contracts and their tests are maintained in [PROTOCOL.md](PROTOCOL.md) and [LIFECYCLE.md](LIFECYCLE.md#executable-acceptance-tests); historical handoffs do not override them.

## Files outside the runner package

These are all changed paths outside `codex-rs/mcp-console-sandbox/`. M modifies an upstream file; A adds a file. Paths are repository-relative.

| Path                                                                                           | Change and upgrade review                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M [codex-rs/Cargo.toml](../Cargo.toml)                                                         | Registers the workspace member. Match the new release's workspace membership and inherited dependencies.                                                                                                                                                                                                                                                                               |
| M [codex-rs/Cargo.lock](../Cargo.lock)                                                         | Adds the runner and its musl OpenSSL dependency; normalizes workspace versions from `0.0.0` to `0.154.0`. External packages are unchanged. Regenerate against the new release rather than replaying version churn.                                                                                                                                                                     |
| M [codex-rs/sandboxing/src/seatbelt.rs](../sandboxing/src/seatbelt.rs)                         | Exposes `SeatbeltProfile` and `create_seatbelt_profile` for the CLI encoder and runner. Review every `CreateSeatbeltCommandArgsParams` field, process-profile selection, and symlink opt-in propagation. The runner consumes the structured result directly.                                                                                                                           |
| M [codex-rs/linux-sandbox/src/lib.rs](../linux-sandbox/src/lib.rs)                             | Adds `TargetSetupHook`, `TargetSetupMode`, and `run_main_with_target_setup`; ordinary `run_main` remains available. The post-enforcement hook owns setup and returns an optional close-on-exec control endpoint for namespace init; direct mode returns none.                                                                                                                          |
| M [codex-rs/linux-sandbox/src/linux_run_main.rs](../linux-sandbox/src/linux_run_main.rs)       | Carries `--target-setup-fd`, invokes the hook, restores transferred stdin, and closes waiting helpers' readers. Namespace init enters the control loop; direct mode execs. Preserve enforcement/proxy order, proxy descriptors, fresh/inherited procfs, and signal restoration after subprocess-library resets. Target spawning and stdin changes also affect ordinary native callers. |
| M [codex-rs/linux-sandbox/src/launcher.rs](../linux-sandbox/src/launcher.rs)                   | Preserves original stdin through an extra descriptor and `--stdin-fd`, leaving the host bubblewrap monitor on null stdin. Review `--apply-seccomp-then-exec`, descriptor ownership, host-helper precedence, bundled verification, `--as-pid-1`, and re-exec without `--argv0`.                                                                                                         |
| A [codex-rs/linux-sandbox/src/target_control.rs](../linux-sandbox/src/target_control.rs)       | Namespace init waits on control/SIGCHLD events, forwards HUP/INT/QUIT/TERM to its known child, and reaps children. Target exit returns its status; channel EOF exits PID 1 so the kernel retires the namespace. Review `signalfd`, descriptor I/O, status propagation, and the native monitor's completion barrier. Control must stay out of the workload.                             |
| A [.github/workflows/mcp-console-sandbox.yml](../../.github/workflows/mcp-console-sandbox.yml) | Defines macOS/GNU Linux executable and native checks, Bazel/lints, and both musl artifact contracts. Review toolchain/namespace setup, helper strip/hash/build order, and musl-only environment scoping. GNU interposer tests need their dynamic host toolchain.                                                                                                                       |

## Runner modules and native assumptions

All remaining additions are in this package: the modules below, [Cargo.toml](Cargo.toml), [BUILD.bazel](BUILD.bazel), [tests](tests), [fixtures](src/bin), [caller example](examples/bootstrap.py), and documentation. Cargo and Bazel must include the same tests and interposer resources. The native crate's existing Bazel source glob includes `target_control.rs`.

| Modules                                                                                  | Responsibility and upgrade review                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [main.rs](src/main.rs), [bootstrap.rs](src/bootstrap.rs), [config.rs](src/config.rs)     | Own input modes and validation. Dispatch native invocations before ordinary parsing and descriptor hygiene. Review leading helper arguments, descriptor validation before allocation, and the protocol's acceptance-time integrity boundaries.                                                                                                           |
| [codex.rs](src/codex.rs)                                                                 | Centralizes upstream policy, Linux `SandboxManager`, structured Seatbelt preparation, and `RemoteNetworkProxyConfig` / `NetworkProxy`. Review wire types, permission conversion, proxy lifetime and namespace-local endpoints. Keep target environment separate from trusted helper setup; compose private-storage permissions and macOS additions here. |
| [native.rs](src/native.rs), [direct_linux.rs](src/direct_linux.rs)                       | Apply target environment and signals after enforcement. macOS calls `sandbox_init_with_parameters` and execs. Linux retains control only in namespace init; explicit Landlock reads sealed anonymous setup storage, closes it, and execs. Review the macOS ABI, hook modes, close-on-exec, signal reset order, and Landlock checks.                      |
| [launch.rs](src/launch.rs), [platform.rs](src/platform.rs), [signals.rs](src/signals.rs) | Sequence preparation through cleanup. Preserve current-thread spawning for Linux death links, unreaped root observation with `waitid(WNOWAIT)`, complete target signal restoration, and cancellation checks before setup writes.                                                                                                                         |
| [platform_linux.rs](src/platform_linux.rs)                                               | Observe the direct parent, receive readiness credentials, forward control, optionally capture a pidfd, and wait for the known native child. Review `SO_PASSCRED` / `SCM_CREDENTIALS`, glibc/musl message layouts, pidfd-unavailability errors, and creating-thread death signals.                                                                        |
| [platform_macos.rs](src/platform_macos.rs)                                               | Track PID/start-time identities with libproc/kqueue, retire observed descendants and the original group, and manage eligible foreground terminals. Preserve root identity and the documented observation/PID-reuse limits.                                                                                                                               |
| [storage.rs](src/storage.rs)                                                             | Remove the private container only after established retirement. Review descriptor-relative no-follow traversal, mode-zero directories, and Linux procfs-backed permission changes. Storage is separate from upstream mount bookkeeping.                                                                                                                  |

## Procfs, control, and lifetime review

[LINUX_COMPATIBILITY.md](LINUX_COMPATIBILITY.md) owns the fresh/inherited procfs comparison, runtime prerequisites, and downstream differential evidence. The runner's injected identity test checks acceptance only. Procfs remains necessary for outer descriptor hygiene, storage permission changes, native setup, and verified descriptor execution; removing host process discovery did not make execution procfs-free.

Readiness obtains namespace init's host identity from kernel credentials. Preserve the optional pidfd path and native-channel retirement, including explicit failure when native wait status is missing or the deadline expires. The [lifecycle contract](LIFECYCLE.md#boundaries-and-intentional-differences) defines these outcomes. Init uses descriptor `read`/`write` permitted by the unchanged restricted-network filter; the host configures credentials before launch. The control endpoint never becomes a policy-update interface or target descriptor.

Review Linux's direct-child creating thread, namespace-init re-arming after credential/exec transitions, native TERM links in [proxy_lifecycle.rs](../linux-sandbox/src/proxy_lifecycle.rs), synthetic-mount supervision, and [vendored bubblewrap](../vendor/bubblewrap/bubblewrap.c). Preserve the [runner-loss startup windows](LIFECYCLE.md#owned-stdio-process-groups-and-linux-parent-death) even when death-chain tests pass.

The independent observers in [ownership_tests.rs](tests/lifecycle/ownership_tests.rs) use pidfds, host child lists, and `NSpid` to verify exits and clean failed fixtures. These are test-host requirements, not runner prerequisites. Review fault-interposer checkpoints against actual syscall/helper boundaries without substituting sleeps or retries. [README.md](README.md#build-and-validation) owns packaging instructions; [REBASE.md](REBASE.md) owns upgrade validation and revision-specific evidence.
