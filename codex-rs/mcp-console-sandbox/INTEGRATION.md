# Upstream integration and rolling patch maintenance

This continuation starts at `3ee7d3190983b482b312ddfc3201c464179a1245` on the extraction branch for `rust-v0.150.1` (`90854393966b21e9ebfd21b122334eb09a20c93d`). The earlier workspace/package registration and transport patches are recorded in [REBASE.md](REBASE.md). Only this pinned release is supported.

The [capability inventory](CAPABILITIES.md) describes the complete branch's additions over upstream. The integration table below includes the earlier Linux stdin changes as well as the lifecycle continuation. The earlier socket-operation relaxation has been removed; `linux-sandbox/src/landlock.rs` matches the pinned release exactly.

[PR266.md](PR266.md) records the subsequent retirement, startup-cancellation and Linux prerequisite fixes, their red/green evidence, host validation limits and remaining Console work. Those changes retain the integration surface below. In the Linux native hook, expected procfs/setup failures now return a diagnostic and nonzero exit instead of panicking; the prerequisite rejection and enforcement order remain unchanged.

## Self-contained additions

The runner owns configuration, signal capture/restoration, lifecycle sequencing, native gate payloads, temporary storage, and platform kernel operations in its own `src/` files. `launch.rs` shares sequencing; `platform_macos.rs` and `platform_linux.rs` contain only the operations that differ. Tests, fixtures, the protocol, and [behavior mapping](LIFECYCLE.md#executable-acceptance-tests) stay in this package.

`src/codex.rs` is the only integration module. It translates the owned request into the pinned native filesystem policy, `PermissionProfile`, `SandboxManager` transformation, structured Seatbelt profile, and `RemoteNetworkProxyConfig`/`NetworkProxy` lifetime. Configuration exposes the existing upstream filesystem/network/proxy wire types; these remain internal-version dependencies despite the executable boundary. No lifecycle code imports upstream orchestration types. The native setup payload is private to one launch, not a second public protocol or an abstraction framework.

## Modifications outside this package

| File                                  | Necessary extension and assumptions                                                                                                                                                                                                                                                                                                                                                                 | Validation/manual upgrade review                                                                                                                                                                                                                                                                                                |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sandboxing/src/seatbelt.rs`          | Exposes `SeatbeltProfile` and `create_seatbelt_profile` so the native stage can apply the existing policy and parameter pairs directly. The existing sandbox-exec API encodes the same prepared result. Avoids parsing CLI shapes, copying SBPL generation, or keeping a signal wrapper after sandbox-exec.                                                                                         | Native Seatbelt suite; executable filesystem, proxy, extension, descriptor, signal, and target-loader tests. Review `CreateSeatbeltCommandArgsParams`, profile selection, parameter semantics, and the macOS sandbox initialization ABI.                                                                                        |
| `linux-sandbox/src/lib.rs`            | Adds `run_main_with_target_setup`, one function-pointer hook taking a target `Command` and an owned private descriptor. Ordinary `run_main` remains available.                                                                                                                                                                                                                                      | The executable's native-boundary and signal tests. Keep the hook explicitly after native enforcement and before target spawn.                                                                                                                                                                                                   |
| `linux-sandbox/src/linux_run_main.rs` | Carries `--target-setup-fd` through native namespace creation, rejects legacy/no-proc modes for that hook, verifies namespace-local procfs, and closes the descriptor through the hook. Uses standard subprocess spawning at the final target boundary so the hook can restore signals after library resets. Existing namespace-init waitpid/reaping and forwarding remain native responsibilities. | Linux sandbox/bwrap suites; loader-constructor denial, framed-fd isolation, inherited signals, stdin closure, caller death, terminal INT, proxy routing, and supervisor-loss tests. Review descriptor preservation across bubblewrap, procfs placement, PID namespace creation, proxy environment rewriting, and hook ordering. |
| `linux-sandbox/src/launcher.rs`       | Carries the original stdin through an extra preserved descriptor so bubblewrap's host monitor does not retain a reader. `linux_run_main.rs` restores it at the inner stage and releases waiting helpers' copies. Required for caller-visible target stdin closure without a relay or vendored bubblewrap patch.                                                                                     | Executable `transport::waiting_executable_releases_target_stdin`, regular-file offset/shared-seek and terminal-identity tests, and native Linux suites. Review the inner-stage marker, preserved-descriptor handling, and every waiting helper's stdin ownership.                                                               |

The upstream seccomp deny rules, proxy implementation, vendored helper, shared protocol model, and MCP Console files remain unchanged. Optional private-storage permissions and caller-supplied Seatbelt extensions are composed in the runner. Workspace registration and Cargo lock changes are listed in [REBASE.md](REBASE.md#existing-upstream-files).

Native helper dispatch retains the earlier extraction's documented leading-option convention. It is an upgrade review point, not an inferred process-layout contract. Linux readiness obtains the native init's host identity from socket credentials and pins it with a pidfd; it does not search for a particular helper tree.

The gated native endpoint uses `File` read/write on its owned socket descriptor. This avoids the `sendto` syscall used by Linux `UnixStream::write` while retaining kernel-supplied readiness credentials. `SO_PASSCRED` is configured by the host supervisor before native launch. The executable regression `restricted_network_preserves_native_socket_denials_and_descriptor_io` checks all six restored socket-operation denials and successful descriptor I/O on Linux, while retaining macOS behavior. Startup cancellation and caller-death tests exercise the credential-dependent gate with the upstream filter active. Review these assumptions when upgrading Rust or the native filter.

Darwin's libproc/kqueue observation boundary, `sandbox_init_with_parameters`, and Linux's subreaper/pidfd/procfs APIs are platform dependencies. Fault-injection fixtures interpose actual host syscalls to establish causal startup/watch checkpoints and verify failure reporting. They add no production test hooks. Acceptance tests also check that target-side loader constructors run only after enforcement. Do not replace those checks with sleeps or implementation snapshots during rebasing.

## Additional parent-death rebase review

The owned-stdio changes are entirely inside this package. They add no upstream API, public lifecycle option, dependency, helper process, or mutable configuration. Review these assumptions along with the four existing integration files:

- `launch.rs` captures the original terminal group before `setpgid`, selects isolation from a validated parent and nonterminal stdin/stdout, and leaves the native process group/session arrangement intact. Directed signal forwarding remains separate from signals sent to the caller's group.
- The current-thread runtime in `main.rs` keeps the native child's creating thread alive until retirement. Moving spawn into a short-lived worker thread would break Linux's parent-death contract. The pre-exec hook must continue to capture the expected parent before fork, arm SIGKILL, and check parenthood without allocation or locks.
- Trusted native helpers need unblocked forwarding signals for the existing TERM death links in `linux-sandbox/src/proxy_lifecycle.rs` and synthetic-mount supervision in `linux_run_main.rs`. Original caller dispositions/masks belong at the final target hook.
- `launcher.rs` selects system or bundled bubblewrap, uses `--as-pid-1`, and preserves `--die-with-parent`. Check every native fork, exec, credential change, and creating thread. The leaf `native.rs` hook re-arms SIGKILL in namespace init after exec and before readiness; init persists while the target and detached descendants execute. A final target exec must not replace that init.

The [Linux man page](https://man7.org/linux/man-pages/man2/PR_SET_PDEATHSIG.2const.html) documents fork, credential, exec, and creating-thread limits. The reviewed bundled implementation arms the host monitor after dropping privileges and the namespace command before exec (`vendor/bubblewrap/bubblewrap.c`). Neither inherited settings nor `--die-with-parent` prove coverage of every earlier setup window. The precise remaining startup gap and the absence of storage cleanup after runner death are documented in [LIFECYCLE.md](LIFECYCLE.md#owned-stdio-process-groups-and-linux-parent-death). Setuid/file-capability system helpers were not exercised in this continuation.

Downstream callers need to repin and rebuild the tested executable. Protocol 2, configuration, policy, and the ordinary caller relationship are unchanged. A caller that previously relied on broadcasting INT/HUP/QUIT/TERM to its own group must instead address the runner when it wants forwarding. MCP Console changes are outside this patch.

## Reapplication

1. Start from the new release and reapply the focused extraction history. Keep the native hook edits separate from leaf lifecycle/tests/docs so conflicts remain reviewable. Update workspace registration/locks only as needed by the new release.
2. Review the four native integration files and the private facade against the new APIs. Verify the hook still runs after filesystem/network enforcement, procfs is namespace-local, no setup descriptor reaches target code, and final signal restoration follows standard-library resets. Preserve native proxy endpoint translation when applying deferred target environment variables. Recheck waiting-helper stdin ownership, descriptor I/O under the upstream filter, and kernel-supplied readiness credentials.
3. Run the complete executable suite on macOS and Linux with retries disabled against debug and release binaries. Build ordinary bubblewrap on Linux and supply its path as `CARGO_BIN_EXE_bwrap`. Run `just test -p codex-sandboxing` on macOS and `just test -p codex-sandboxing -p codex-linux-sandbox -p codex-bwrap --retries 0` on Linux. The two established macOS stderr-matcher exclusions are documented in REBASE; reconsider them when upgrading.
4. Run the localized Bazel test, scoped Clippy, `just fix` for changed packages, `just fmt`, and `git diff --check`. If dependencies change, run `just bazel-lock-update`. Inspect the complete upstream diff for unintended enforcement changes. Hosted workflow results are separate evidence.

## Validation of this continuation

Local native validation uses macOS arm64 and the existing Ubuntu 24.04 x86_64 checkout on `mule`, kernel 6.8.0-139, with the pinned Rust 1.95.0 toolchain. Linux's AppArmor setting blocks the locally built bubblewrap's network namespace initialization unless its execution uses the host's existing `bwrap` profile. The executable tests use a task-local compiled launcher that immediately execs `aa-exec -p bwrap -- <ordinary-built-bwrap>`; no host security setting or repository helper implementation is changed. Native Linux tests use the installed host bubblewrap directly. This host accommodation is not a shipped executable dependency.

The debug and release executable suites passed 54 tests on macOS and 52 on Linux, with retries disabled. The native suites passed 92 tests on macOS with the two established exclusions, and all 207 tests on Linux. The macOS Bazel executable suite passed. Scoped Clippy, `just fix`, argument-comment lint on macOS, `just fmt`, and `git diff --check` passed. `just bazel-lock-update` completed without lockfile changes; the leaf explicitly selects Tokio's runtime, timer, and macro features without changing dependency versions.

The Linux Bazel build passed, but its full executable test run is limited by this host's AppArmor policy: the actual bundled helper fails network namespace setup with `loopback: Failed RTM_NEWADDR: Operation not permitted`. Replacing that verified helper with the Cargo test launcher correctly fails the native digest check. Neither verification nor host security settings were weakened to obtain a green result. The Cargo debug/release suites exercise the same Linux supervisor and native Rust hook using the ordinary helper through the existing AppArmor profile. A namespace-enabled CI host is still needed for the unmodified Linux Bazel runtime gate. The focused hosted workflow has not been run as part of this local task.

## Validation after restoring upstream socket rules

The executable regression first failed because all six socket operations succeeded under the relaxed policy. Restoring upstream `landlock.rs` then exposed `native target setup: Operation not permitted` before target execution. Changing the native endpoint to descriptor read/write made the regression pass, including successful local socket-pair I/O and all six expected `EPERM` results on Linux.

The complete debug and release executable suites passed 52 tests each on Linux and 54 each on macOS, with retries disabled. All 207 Linux native tests and the macOS Bazel executable suite passed. The macOS debug run reported five Nextest output-descriptor `LEAK` annotations despite passing assertions; the release run had none. Their cause was not isolated by this change. Linux Cargo tests used the same test-local AppArmor launcher described above. The Linux Bazel runtime gate was not repeated; its documented host restriction remains a validation limitation.

Scoped `just fix` passed on both platforms; the macOS argument-comment lint and `just fmt` on both platforms passed. Native `landlock.rs` is byte-for-byte identical to release commit `90854393966b21e9ebfd21b122334eb09a20c93d`. No dependency or lockfile changes were needed. [MCP_CONSOLE_HANDOFF.md](MCP_CONSOLE_HANDOFF.md) specifies the downstream transport and lifecycle integration work.

## Validation of owned stdio isolation and Linux parent death

This change starts at `4569dae82`, without resetting the branch. Before implementation, the new group-kill contract left native processes alive on both platforms. Linux runner SIGKILL also left native processes alive. Adding only the direct-child death signal still left namespace init alive, and proxy-enabled startup exposed bridges with blocked TERM. Unblocking the trusted helper's supervisor mask and re-arming namespace init made those contracts pass. A separate syscall interposer measured `PDEATHSIG=0` immediately before the native hook on the AppArmor host; the hook now arms SIGKILL there. No vendored or upstream integration source changed.

Validation used macOS arm64 and Ubuntu 24.04 x86_64 on `mule` (kernel 6.8.0-139), with Rust 1.95.0 and retries disabled:

| Check                                               | Result                                                                                                                               |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| macOS debug / release executable contracts          | 61 / 61 passed                                                                                                                       |
| Linux debug / release executable contracts          | 62 / 62 passed                                                                                                                       |
| Linux namespace-enabled container, ordinary helpers | All 62 debug executable contracts passed; system bubblewrap 0.9.0 and the unmodified bundled helper, with proxy enabled and disabled |
| Native suites                                       | Linux: all 207 passed. macOS: 92 passed with the two established stderr-matcher exclusions in REBASE                                 |
| macOS Bazel executable contract                     | Passed                                                                                                                               |

Linux host executable contracts use the existing task-local AppArmor launcher for the bundled executable, as described above; the system selection uses installed bubblewrap. The container runs with namespace prerequisites enabled and uses the ordinary bundled executable directly. Less permissive container attempts failed native namespace/procfs prerequisites before workload readiness; the successful container run did not weaken those checks. An initial Linux native-suite invocation put the unprofiled built helper on PATH and failed 12 cases at uid-map setup; restoring the ordinary host PATH passed all 207. No host policy or sysctl changed.

Some passing macOS debug/native runs reported Nextest output-descriptor `LEAK` annotations, including `cwd_and_complete_environment_reach_the_target`; release contracts had none. Their cause remains unisolated. Setuid/file-capability system helpers, Windows, Linux Bazel runtime, and hosted CI were not validated. The internal bubblewrap startup windows described in LIFECYCLE remain outside the verified termination guarantee.

Scoped `just fix` and all-target Clippy with warnings denied passed on both platforms. The macOS argument-comment lint, `just fmt`, and `git diff --check` passed. Functional tests preceded the final lint/format pass. No dependency or lockfile update was needed.
