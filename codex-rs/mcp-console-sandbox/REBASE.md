# Reapplying the native sandbox extraction

[INTEGRATION.md](INTEGRATION.md) inventories changed paths, runner modules, and upstream assumptions. [LIFECYCLE.md](LIFECYCLE.md#executable-acceptance-tests) maps behavior to executable tests. Use both with the source when moving to another release.

## Procedure

1. Fetch the latest extraction branch and record its exact tip. The current base is `rust-v0.154.0`, commit `6b9826e3aa83b1a5947db50f4332cb9c65f1b340`. Inspect the complete base-to-tip diff, including subsequent compatibility and artifact work:

   ```sh
   git fetch origin mcp-console/sandbox-runner/rust-v0.154.0
   sandbox_tip=$(git rev-parse origin/mcp-console/sandbox-runner/rust-v0.154.0)
   git diff --name-status 6b9826e3aa83b1a5947db50f4332cb9c65f1b340 "$sandbox_tip"
   git diff 6b9826e3aa83b1a5947db50f4332cb9c65f1b340 "$sandbox_tip"
   ```

2. Start a new branch/worktree at the next exact upstream release. Keep the existing extraction branch and its history intact. Reapply the current net change against the new interfaces in small commits. Historical commits include removed policies, requirements, and process layers; replaying the entire history is not the specification. Keep native integration edits, the runner/tests, and build registration/artifacts reviewable separately.
3. Review every integration row against the new source and its callers. Preserve the [protocol](PROTOCOL.md) and [lifetime boundaries](LIFECYCLE.md), including inherited procfs, optional pidfds, configured caller death versus runner loss, and incomplete-retirement reporting. Do not restore the old fresh-procfs gate, host discovery, Console manager, or target signal wrapper. Keep the acceptance-time integrity distinction between environment and descriptor configuration.
4. Carry forward the executable tests and deterministic fixtures with the implementation. Follow the validation checklist below, recording actual revisions, artifacts, host capabilities, skips, and failures. Recheck both ordinary native entry points and the runner's hooks. Test-observer requirements must not become production prerequisites.
5. Regenerate Cargo lock state against the new workspace. If Rust dependencies change, run `just bazel-lock-update` from the repository root and review `MODULE.bazel.lock` with `Cargo.lock`. Inspect the complete new upstream diff for accidental enforcement or dependency changes; update the base revision and inventory from that diff. Verify documentation links, paths, symbols, and test selectors, then format changed Markdown with `yamark format --wrap paragraph` and run `git diff --check`.
6. Commit and push new commits without amending or force-pushing. Report local and hosted validation separately. Downstream adoption is a separate task: select an exact tested revision and rebuild its artifacts; an old handoff pin is not an instruction to downgrade.

## Validation checklist

The executable commands and job definitions live in [.github/workflows/mcp-console-sandbox.yml](../../.github/workflows/mcp-console-sandbox.yml); use its current commands rather than copying a historical transcript. [README.md](README.md#build-and-validation) describes builds, artifact staging, and Cargo target-directory resolution. [LINUX_COMPATIBILITY.md](LINUX_COMPATIBILITY.md) distinguishes native host capabilities from test-fixture prerequisites.

- On macOS and GNU Linux, run the complete executable suite with `just test --locked -p codex-mcp-console-sandbox --retries 0` against debug and release runners. On Linux, build ordinary bubblewrap first and set `CARGO_BIN_EXE_bwrap` to the matching artifact. For release contracts also set `CARGO_BIN_EXE_mcp-console-sandbox`. Keep interposer tests on dynamically linked executables.
- Run `just test -p codex-sandboxing` on macOS and `just test -p codex-sandboxing -p codex-linux-sandbox -p codex-bwrap --retries 0` on Linux. The workflow adds the built debug helper to PATH only for native Linux tests because an existing fallback fixture copies it. Review the macOS exclusions below against the new release.
- Run `bazel test //codex-rs/mcp-console-sandbox:bootstrap-contract-test` on both platforms. A host-policy or dependency-download failure is a validation limitation, not a passing runtime gate. Record any test-only AppArmor accommodation separately from ordinary-helper results; do not change enforcement to obtain a pass.
- Preserve the workflow's x86_64 and aarch64 musl builds, helper strip/hash order, build-scoped toolchain variables, ELF dependency checks, and static-artifact `transport::` / `lifecycle::` contracts. These checks complement GNU fault-injection coverage. They do not establish portability of a target's own runtime or libraries.
- Review the [procfs differential tests](LINUX_COMPATIBILITY.md#procfs-evidence) on a host that can exercise real fresh and inherited views. The runner's injected identity test alone does not establish their visibility/policy contract. Keep downstream integration results tied to the tested downstream and runner revisions.
- Run scoped Clippy and `just argument-comment-lint -p codex-mcp-console-sandbox`. For implementation changes, run `just fix -p` for the changed packages and `just fmt` from `codex-rs` after functional tests; do not rerun tests solely because fix/format ran. Follow the repository's approval rule before any full-workspace suite.

The workflow currently excludes `create_seatbelt_args_with_read_only_git_and_codex_subpaths` and `create_seatbelt_args_with_read_only_git_pointer_file` from the macOS native suite. Both were reproduced at the pinned upstream base: the operation was denied, but the stderr matcher rejected Bash's `line 1:` prefix. These are existing host-specific assertion failures, not policy exemptions. Reconsider the exclusions on each upgrade; all runner contracts still run.

## Validation of the 0.154.0 reapplication

On 2026-09-10, the net patch from `mcp-console/sandbox-runner/rust-v0.150.1` at `561883728ee639c846b80280fa4ed3ff41d12d69` was reapplied to `rust-v0.154.0`, commit `6b9826e3aa83b1a5947db50f4332cb9c65f1b340`, on `mcp-console/sandbox-runner/rust-v0.154.0`. The original patch branch and its history were preserved. The reapplied runtime and contracts are recorded at `e46a1567910094edc526095b778993b1b3ab67c0`.

Workspace registration adds the standalone runner to the current release's members. The shared Seatbelt profile builder propagates the release's new symlink option. Linux supervision retains both upstream's proxy-control descriptor cleanup and the patch's stdin cleanup. Runner runtime files are unchanged from the source patch. The caller-death cleanup assertion now includes captured runner stderr when private storage remains.

Cargo regenerated the workspace package versions to `0.154.0` and added the runner. All external lock entries, including dependency lists and checksums, match the release. `just bazel-lock-update` completed without changing `MODULE.bazel.lock`.

| Host                                                         | Debug runner contracts     | Release runner contracts | Native sandbox suites                                           | Bazel runner contracts |
| ------------------------------------------------------------ | -------------------------- | ------------------------ | --------------------------------------------------------------- | ---------------------- |
| macOS 26.6.2, aarch64                                        | 68 passed                  | 68 passed                | 101 passed, 2 failures reproduced at the unmodified release tag | 68 passed              |
| Local Ubuntu container, aarch64, kernel 6.8.0-117-generic    | 77 passed on the final run | 77 passed                | 219 passed, 1 internal helper ignored                           | Not run                |
| Ubuntu container on `mule`, x86_64, kernel 6.8.0-139-generic | 77 passed                  | 77 passed                | 219 passed, 1 internal helper ignored                           | Not run                |

Cargo builds used Rust 1.95.0, and executable and native tests ran through `just test` with retries disabled. Linux tests used ordinary system and bundled bubblewrap in privileged Docker containers with namespace operations available. The source checkout must be writable and owned by the test user because the native suite creates temporary filesystem fixtures there. Initial runs with a read-only checkout, and with mismatched ownership on x86_64, failed those fixture operations; the table records runs after correcting the container setup. The ignored native test is `suite::managed_proxy::handoff_client`, which is invoked by its enclosing integration test.

The two macOS native failures remain the Bash `line 1:` stderr-matching failures described above. One passing macOS debug contract, `configuration::descriptor_environment_cannot_export_reserved_transport`, received a Nextest LEAK annotation. The macOS release suite and Bazel suite passed. The first ARM debug run failed `ownership::caller_sigkill_retires_workload_and_storage` with retained storage; its isolated rerun and subsequent full debug and release suites passed. The cause of these intermittent observations remains unresolved; the added assertion diagnostic exposes the runner's error if storage retention recurs.

Scoped `just fix` passed for the runner, sandboxing, and Linux sandbox packages on macOS and Linux. The runner's argument-comment lint, workflow Actionlint, `just fmt`, Markdown formatting, and `git diff --check` passed on macOS. Linux Bazel, musl artifact builds, hosted CI, and downstream adoption were not run for this reapplication.

## Historical validation records

These revision-specific records preserve their original results and limitations. Their counts, pins, architecture descriptions, and remaining-work lists apply only to the revisions recorded there. They are not current instructions or validation of today's branch.

| Stage and record revision                                                                                                                                                                                                                                                                                                                                                                                                  | Evidence and limits                                                                                                                                                                                                                                                          |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [One-shot correction at a3980faf9](https://github.com/t-kalinowski/codex/blob/a3980faf9ca5bb302d31b5937e20b7931adc31ff/codex-rs/mcp-console-sandbox/REBASE.md), [Linux follow-up at 3b84ecdc4](https://github.com/t-kalinowski/codex/blob/3b84ecdc4cc3149722fa1aa96648c78940285ecc/codex-rs/mcp-console-sandbox/REBASE.md)                                                                                                 | Protocol-1 extraction and Linux concurrent-staging regression. The Linux record distinguishes initial namespace/libcap failures, later host setup, Cargo/Bazel results, and unrun hosted CI. Its caller-owned lifetime description predates runner supervision.              |
| [Seatbelt extension at 093828701](https://github.com/t-kalinowski/codex/blob/093828701a5206b2a17efc723917c86164e522fd/codex-rs/mcp-console-sandbox/REBASE.md#caller-supplied-macos-profile-extension)                                                                                                                                                                                                                      | macOS extension tests and checks; Linux rejection and hosted CI were not run there. The later structured-profile hook supersedes its command-line composition description.                                                                                                   |
| [Descriptor transport record at 4a061c4ec](https://github.com/t-kalinowski/codex/blob/4a061c4ec94f5ad99e148982168ea4f21367c26c/codex-rs/mcp-console-sandbox/REBASE.md#dedicated-bootstrap-descriptor-on-2026-09-08)                                                                                                                                                                                                        | Implementation `cdce4a6db`; macOS/Linux debug, release, Bazel and binary-input results. Retains the two macOS native assertion failures and unexplained Nextest LEAK annotations. No hosted CI; the later environment interface and runner lifecycle are absent.             |
| [Lifecycle record at 6b6d90141](https://github.com/t-kalinowski/codex/blob/6b6d901416ab97ebb53d2ebb978f118c84f2ce32/codex-rs/mcp-console-sandbox/INTEGRATION.md#validation-of-this-continuation), [socket-rule restoration at 633cd93fc](https://github.com/t-kalinowski/codex/blob/633cd93fc1626060c7186386064a8ef06a25a271/codex-rs/mcp-console-sandbox/INTEGRATION.md#validation-after-restoring-upstream-socket-rules) | Lifecycle implementation `4dd415160` / tests `6446aa931`; socket restoration `270b25515`. Linux Cargo used a test-only AppArmor launcher; Linux Bazel runtime remained blocked, hosted CI was unrun, and passing macOS runs included unexplained LEAK annotations.           |
| [PR #266 fixes](https://github.com/t-kalinowski/codex/blob/29b89360756250c94c81005e8902ab8882a8404d/codex-rs/mcp-console-sandbox/PR266.md)                                                                                                                                                                                                                                                                                 | Revision-specific root-retirement and startup-cancellation red/green evidence through `fe1b9e4e8`. Its procfs refusal was subsequently removed. Includes native stderr failures, LEAK annotations, and unvalidated Linux nested-procfs/Bazel and downstream work.            |
| [Owned stdio and parent death at b5a1c9f76](https://github.com/t-kalinowski/codex/blob/b5a1c9f76a9c6ca2909105aecbc78555a26fda01/codex-rs/mcp-console-sandbox/INTEGRATION.md#validation-of-owned-stdio-isolation-and-linux-parent-death)                                                                                                                                                                                    | Caller/group loss, runner loss and startup checkpoints, including ordinary system/bundled helpers in a namespace-enabled container. Earlier internal startup windows, privileged helpers, Linux Bazel runtime, hosted CI, and macOS LEAK annotations remain explicit limits. |
| [Linux compatibility at downstream f21abe426](https://github.com/t-kalinowski/mcp-console/blob/f21abe426014ba70ef2a1ab7419955396d385aab/docs/LINUX_COMPATIBILITY.md#validation-record)                                                                                                                                                                                                                                     | Coordinated runner `7aacbcf1b` and downstream policy/installation results. Linux hosted CI stopped at an external dependency download before Bazel compilation; subsequent release/lint steps were skipped. This predates the musl artifact commits.                         |
