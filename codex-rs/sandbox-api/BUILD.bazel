load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "sandbox-api",
    crate_name = "codex_sandbox_api",
    extra_binaries = [
        "//codex-rs/bwrap:bwrap",
    ],
    test_tags = ["no-sandbox"],
)
